import { createStore } from 'vuex'

export default createStore({
    state: {
        // Здесь можно определить начальное состояние, если это необходимо
    },
    mutations: {
        // Здесь можно определить мутации для изменения состояния
    },
    actions: {
        // Здесь можно определить действия для выполнения асинхронных операций
    },
    modules: {
        // Здесь можно разделить хранилище на модули, если приложение становится сложным
    }
})
